package main

import "nasbot/internal/model"

// Type aliases to internal model package
type VolumeStats = model.VolumeStats
type Stats = model.Stats
type ProcInfo = model.ProcInfo
type ContainerInfo = model.ContainerInfo
type DiskUsagePoint = model.DiskUsagePoint
type ReportEvent = model.ReportEvent
type StressTracker = model.StressTracker
type DiskPrediction = model.DiskPrediction
type TrendPoint = model.TrendPoint
type DockerCache = model.DockerCache
